# ROADMAP — 后续优化规划

本文档记录项目的未来优化方向和想法。具体开发时转为 GitHub Issues 跟踪。

---

## 自动监控

### 高优先级

- [ ] **多收藏夹监控** — 目前仅支持单个收藏夹，改为列表形式，每个收藏夹可独立配置间隔、目录、下载内容
- [ ] **Cookie 过期自动暂停** — 检测到 API 返回未登录错误时，自动暂停轮询并通过状态栏通知用户，防止无效请求浪费配额
- [ ] **收藏夹页面一键启用监控** — 在 `ViewPublicFavorites` 页面添加按钮，浏览收藏夹时直接开启/关闭对该收藏夹的监控，免去手动输入 mediaId
- [ ] **定时检查而非周期检查** — 支持指定具体时间点（如每天 02:00）而非固定间隔

### 中优先级

- [ ] **增量请求优化** — 当前用 `GetFavoritesMediaId` 一次性拉取全部 ID（单次请求），若收藏夹视频数极大可考虑基于时间戳的增量查询
- [ ] **监控统计面板** — 在设置页或状态栏显示「已自动下载 X 个视频 | 上次检查 X 分钟前 | 下次检查 X 分钟后」
- [ ] **启动时跳过重复下载** — 服务刚启动的即时检查可能会发现大量"新"视频（实际是之前手动下载过的），需要对比下载历史而不仅仅是 `auto_watch_record`

### 低优先级

- [ ] **过滤规则** — 支持按关键词、UP主名称、视频时长等条件过滤，不符合的视频不自动下载
- [ ] **下载完成通知** — 支持 Webhook 推送（钉钉机器人 / 企业微信 / Telegram Bot），下载完成后自动通知

---

## 项目整体

### 高优先级

- [x] **文件命名增加下载时间选项** — 在「设置 → 视频 → 文件命名格式」中增加「下载时间（yyyy-MM-dd）」选项，支持按下载日期在最外层创建文件夹，实现类似 `E:\DownKyiMedia\2026-05-30\视频标题.mp4` 的目录结构（**已完成**）
- [ ] **自动化测试** — 目前零测试。建议给 `DownKyi.Core` 添加 xUnit 单元测试，优先覆盖：
  - 设置读写（`SettingsManager`）
  - API 解析（`BvId` / `ParseEntrance`）
  - 文件名格式化（`FileName`）
  - 自动监控增量检测逻辑
- [ ] **多语言支持** — 目前仅有中文（`Languanges/Default.axaml`），需要添加英文 `en.axaml` 并使 `DictionaryResource.GetString()` 支持语言切换

### 中优先级

- [ ] **代码质量工具** — 添加 `.editorconfig`、启用 Roslyn 分析器，统一代码风格
- [ ] **日志文件轮转** — 当前日志文件会无限增长（`20260530(0).log`），需加自动清理策略（保留最近 N 天或限制文件大小）
- [ ] **设置导入导出** — 支持将设置导出为 JSON 文件并导入，方便多设备同步
- [ ] **下载历史清理** — 已下载记录和 `auto_watch_record` 表定期清理，避免数据库膨胀
- [ ] **应用自更新验证** — RepoOwner/RepoName 已指向本仓库，`AppInfo` 已改为从 `version.txt` 读取。需在 GitHub 创建 Release 后验证自更新流程

### 低优先级

- [ ] **.NET 9/10 升级** — .NET 8 是 LTS（支持至 2026.11），可等 .NET 10 LTS 发布后再升级
- [ ] **CI/CD 完善** — 添加 Windows ARM64 构建、自动测试运行、PR 检查
- [ ] **暗黑模式完善** — 剩余未适配的图标和弹窗
- [ ] **Aria2 启动优化** — 修复多个实例同时启动时 `aira.log` 文件锁定问题
- [ ] **本地开发文档** — 补充 aria2/ffmpeg 本地开发环境搭建说明

---

## 版本规划草案

| 版本 | 主题 |
|------|------|
| v1.0.1 | 文件命名增加下载时间（yyyy-MM-dd） |
| v1.1.0 | 多收藏夹监控 + Cookie 过期检测 + 收藏夹页一键启用 + 定时检查 |
| v1.2.0 | 监控统计面板 + 增量优化 + 过滤规则 |
| v1.3.0 | 自动化测试 + 多语言英文版 |
| v2.0.0 | .NET 10 升级 + CI/CD 完善 |
